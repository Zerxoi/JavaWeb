// import org.apache.el.ExpressionFactoryImpl;
// import org.apache.el.ValueExpressionImpl;
// import org.apache.el.lang.ExpressionBuilder;
// import org.apache.jasper.runtime.PageContextImpl;

// import jakarta.el.ExpressionFactory;
// import jakarta.servlet.http.HttpServlet;
// import jakarta.servlet.http.HttpServletRequest;

public class Test {
    public static void main(String[] args) {
        // ExpressionFactory ef;
        // ExpressionFactoryImpl efi;
        // PageContextImpl pci;
        // pci.proprietaryEvaluate(expression, expectedType, pageContext, functionMap);
        // ExpressionBuilder eb;
        // eb.createValueExpression(expectedType);
        // ValueExpressionImpl vei;
        // vei.getValue(context)
        // HttpServletRequest req = new HttpServletRequest();
        // req.getHeader(name)
        // req.getParameter(name)
        // req.getCookies()
        // new HttpServlet().getServletContext().getInitParameter(name)
    }
}
